---
const { bg, color, onClick } = Astro.props;
---

<button
  class=`py-2 px-4 ${bg} rounded-full gap-2 button flex mx-auto text-center justify-center  lg:w-auto items-center ${color}`
  onclick={onClick}><slot /></button
>
